<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand text-danger" href="<%= root_path%>"><%=t('Twitter')%></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav mr-auto">
            <% if user_signed_in? %>
                <li class="nav-item text-warning">
                <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :get, class:'nav-link'%>
                </li>
                <li class="nav-item text-warning">
                <%= link_to 'Explorar',  all_tweets_path, method: :get, class:'nav-link'%>
                </li>
                <% if current_user.admin == true %>
                <li class="nav-item text-warning">
                <%= link_to 'Admin',  admin_dashboard_path, method: :get, class:'nav-link'%>
                </li>
                <%end%>

            <%else%>
                <li class="nav-item active">
                <%= link_to 'Registro', new_user_registration_path, class:'nav-link text-warning'%>
                </li>
                <li class="nav-item">
                <%= link_to 'Login', new_user_session_path, class:'nav-link text-warning'%>
                </li>
            <%end%>
            </ul>
            
        </div>
        
    </div>
</nav>
